import React from "react";
const CheckBox = () => {
  return (
    <input
      type="checkbox"
      className="checkbox checkbox-success rounded-full  border-2 border-stone-400 hover:border-Primary"
    />
  );
};

export default CheckBox;
